run(object, my_x, my_y, my_range)

call luaCall()
ex checkbomb:
   luaCall(object, "checkBomb", my_x, my_y)

   luaCall(object, "checkCase", x, y) // envoyer x et y de la case ou je veux aller
	return: enum //enum;0 = empty, enum;1 = indestructible, enum;2= destructible
   luaCall(object, "command", enum) // fin de script up=0, down=1, right=2, left=3, bomb=4
   luaCall(object, "radar")
		return: x, y, elem;  elem;0= win(up)  elem;1= ennemy  elem;2=bonus

1er function:
	recoit objet Coor avec 3eme argu (ma porter de bomb)
	call checkbomb(Coor) (bomb dans perimetr) et recoit un Coor
	Si dans ce Coor 3eme argu a zero = pas de bomb
	Do algo....
	else (si bomb)
		3eme argu = range
		x et y = pos bomb
	chaque deplacement = regarder si bomb. While bomb ce deplacer (call checkbomb avec Coor modifier)

	Si pas de bomb -> call radar function (renvoie Coor)
	If Coor 3eme argu == 0 (Win)
	If Coor 3eme argu == 1 (Joueur) x et y = coor jouer
